// API
export { emailServiceAPI } from './api';

// Hooks
export { useEmailServiceAPI } from './hooks/use-email-service-api';

// Types
export type { Audience, AudienceType, Campaign, CampaignStatus, CloudWatchMetric, EmailEnvironment, EmailMetrics, EmailQuickLink, Environment, ListParams, ListResponse, Sender, SESReputationMetrics, SESSendQuota, SESStatusResponse, Setting, Template, Unsubscribe } from './types';

// Export constants
export { EMAIL_QUICK_LINKS, STATUS_COLORS } from './types';
